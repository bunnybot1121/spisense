# @react-three/rapier v2 declares a peer dep on @react-three/fiber v9,
# but this project intentionally runs fiber v8 (React 18) with a custom
# rapier WASM loader (src/rapier-loader.js + vite alias). The combination
# works at runtime, so relax npm's strict peer resolution for installs.
legacy-peer-deps=true
